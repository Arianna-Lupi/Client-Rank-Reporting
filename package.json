{
  "name": "client-rank-reporting",
  "version": "0.1.0",
  "private": true,
  "description": "Slack GSC bot — daily Google Search Console rank reporting for Arianna Lupi",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@upstash/redis": "^1.38.0",
    "googleapis": "^173.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@vercel/node": "^5.8.21",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
