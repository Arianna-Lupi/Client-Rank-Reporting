# Dependencies
node_modules

# Build output
dist

# Vercel
.vercel

# Environment (NEVER commit real secrets)
.env.local
.env*.local

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store

# Service Account keys (NEVER commit)
*.json
!package.json
!tsconfig.json
!vercel.json
!package-lock.json
!vitest.config.json
