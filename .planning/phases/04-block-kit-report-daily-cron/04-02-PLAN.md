---
phase: 04-block-kit-report-daily-cron
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/schedule.ts
  - lib/schedule.test.ts
  - lib/cron-auth.ts
  - lib/cron-auth.test.ts
  - lib/report-lock.ts
  - lib/report-lock.test.ts
autonomous: true
requirements: [SCH-01, SCH-02, PER-02]
must_haves:
  truths:
    - "isReportHour devuelve true solo cuando la hora actual en REPORT_TZ iguala REPORT_HOUR, correcto en frontera DST"
    - "reportDateKey produce el YYYY-MM-DD del día en REPORT_TZ (no en UTC)"
    - "isAuthorizedCron acepta solo Authorization: Bearer <CRON_SECRET> exacto y rechaza ausente/incorrecto"
    - "claimDailyReport reclama el lock una sola vez por día (primera llamada true, segunda false)"
  artifacts:
    - path: "lib/schedule.ts"
      provides: "isReportHour(now, tz, hour) y reportDateKey(now, tz) puros vía Intl"
      contains: "export function isReportHour"
    - path: "lib/cron-auth.ts"
      provides: "isAuthorizedCron(headers, secret) puro"
      contains: "export function isAuthorizedCron"
    - path: "lib/report-lock.ts"
      provides: "claimDailyReport(dateKey, locker?) con SET NX EX inyectable"
      contains: "export function claimDailyReport"
  key_links:
    - from: "lib/schedule.ts"
      to: "Intl.DateTimeFormat con timeZone"
      via: "formateo de hora/fecha sin dependencias"
      pattern: "Intl\\.DateTimeFormat"
    - from: "lib/report-lock.ts"
      to: "redis.set con { nx: true, ex }"
      via: "claim atómico inyectable"
      pattern: "nx:\\s*true"
---

<objective>
Construir las tres primitivas puras/inyectables que el handler del cron compone: el chequeo de hora local DST-safe (SCH-01), la verificación de autorización del cron (SCH-02) y el claim de idempotencia diaria en Redis (PER-02). Sin llamadas vivas: todo se prueba con `now` y deps inyectados.

Purpose: Aislar la lógica de scheduling, seguridad e idempotencia detrás de tests para que el handler de la Wave 2 sea cableado delgado.
Output: `lib/schedule.ts`, `lib/cron-auth.ts`, `lib/report-lock.ts` y sus tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-block-kit-report-daily-cron/04-CONTEXT.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Patrón de inyección de dependencias establecido en Phases 1-3. -->

From lib/clients.ts (patrón de interfaz mínima inyectable):
```typescript
export interface ActiveClientWriter {
  sadd(key: string, ...members: string[]): Promise<number>;
  srem(key: string, ...members: string[]): Promise<number>;
}
```
@upstash/redis `set(key, value, { nx: true, ex: seconds })` devuelve 'OK' cuando escribe (clave nueva) y `null` cuando NX falla (clave ya existía).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: schedule.ts — isReportHour + reportDateKey puros (Intl, DST-safe)</name>
  <files>lib/schedule.ts, lib/schedule.test.ts</files>
  <read_first>lib/metrics.ts (líneas 1-12 estilo de docstring de módulo puro), lib/clients.test.ts (estructura vitest)</read_first>
  <behavior>
    - isReportHour(now, tz, hour) === true cuando la hora local en tz iguala hour; false en cualquier otra hora.
    - DST: un instante UTC fijo que cae en hora local 9 durante horario de verano y un instante distinto que cae en hora local 9 durante horario estándar ambos devuelven true con hour=9 (la hora local sale de Intl, no de un offset fijo).
    - reportDateKey(now, tz) devuelve la fecha local en tz con formato 'YYYY-MM-DD', que puede diferir del día UTC cerca de medianoche.
  </behavior>
  <action>Crear `lib/schedule.ts` con dos funciones puras sin I/O ni acceso a la fecha actual interna. `isReportHour(now: Date, tz: string, hour: number): boolean`: usar `Intl.DateTimeFormat('en-US', { timeZone: tz, hour: '2-digit', hour12: false })` y `formatToParts(now)` para extraer la parte 'hour', convertirla a número (manejar el caso '24' mapeándolo a 0) y comparar con `hour`. `reportDateKey(now: Date, tz: string): string`: usar `Intl.DateTimeFormat('en-CA', { timeZone: tz, year: 'numeric', month: '2-digit', day: '2-digit' })` cuyo `format(now)` ya produce 'YYYY-MM-DD'. Encabezar con docstring que cite SCH-01 y la razón del patrón hourly-cron + tz-check (Vercel Cron es UTC-only). Crear `lib/schedule.test.ts` cubriendo: hora coincidente true, hora no coincidente false, dos instantes UTC en lados opuestos de la frontera DST de America/Mexico_City (o America/New_York) que ambos resuelven a hora local 9 → true, y un caso reportDateKey donde un instante UTC tras medianoche local cae en el día anterior/posterior correcto en tz.</action>
  <verify>
    <automated>npm test -- schedule && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/schedule.test.ts` pasa, incluyendo al menos un caso etiquetado DST
    - `grep -c "Intl.DateTimeFormat" lib/schedule.ts` devuelve >= 1
    - Ningún `new Date()` sin argumentos dentro de las funciones (now siempre es parámetro): `grep -c "new Date()" lib/schedule.ts` devuelve 0
    - `npm run typecheck` verde
  </acceptance_criteria>
  <done>isReportHour y reportDateKey son puras, DST-safe y cubiertas por tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cron-auth.ts — isAuthorizedCron(headers, secret)</name>
  <files>lib/cron-auth.ts, lib/cron-auth.test.ts</files>
  <read_first>api/slack/command.ts (líneas 36-45, patrón de lectura de headers Web-standard y retorno 401), lib/clients.test.ts (estructura vitest)</read_first>
  <behavior>
    - Header `Authorization: Bearer <secret>` exacto → true.
    - Header ausente → false.
    - Header con secret distinto o esquema distinto → false.
    - secret vacío nunca autoriza (defensa): aun con header "Bearer " vacío → false.
  </behavior>
  <action>Crear `lib/cron-auth.ts` con `isAuthorizedCron(headers: Headers, secret: string): boolean`. Leer `headers.get('authorization')`; si es null devolver false. Construir el valor esperado `Bearer ${secret}` y comparar; si `secret` es cadena vacía tras trim devolver false sin comparar. Devolver el booleano de la comparación. Encabezar con docstring que cite SCH-02 y note que Vercel Cron envía este header cuando CRON_SECRET está configurado. No loggear el header ni el secret. Crear `lib/cron-auth.test.ts` con un objeto `new Headers({...})` para cada caso: válido → true, ausente → false, secret incorrecto → false, esquema incorrecto (p. ej. "Basic ...") → false, secret vacío → false.</action>
  <verify>
    <automated>npm test -- cron-auth && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/cron-auth.test.ts` pasa con casos válido/ausente/incorrecto/esquema/vacío
    - `grep -c "console.log\|console.error" lib/cron-auth.ts` devuelve 0
    - `npm run typecheck` verde
  </acceptance_criteria>
  <done>isAuthorizedCron acepta solo el Bearer correcto y está cubierta por tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: report-lock.ts — claimDailyReport(dateKey, locker?) con SET NX EX</name>
  <files>lib/report-lock.ts, lib/report-lock.test.ts</files>
  <read_first>lib/clients.ts (líneas 10-48, patrón Redis.fromEnv lazy + interfaz mínima inyectable + default param), lib/clients.test.ts (fake inyectado que registra llamadas)</read_first>
  <behavior>
    - Primera llamada para un dateKey: el locker.set devuelve 'OK' → claimDailyReport devuelve true.
    - Segunda llamada (clave ya existía): locker.set devuelve null → claimDailyReport devuelve false.
    - La clave usada es `report:posted:<dateKey>` y las opciones incluyen nx:true y un ex de ~129600 segundos (36 h).
  </behavior>
  <action>Crear `lib/report-lock.ts` siguiendo el patrón lazy de `lib/clients.ts`: una constante de prefijo `report:posted:`, una constante `LOCK_TTL_SECONDS = 129600`, una interfaz mínima inyectable `DailyReportLocker` con `set(key: string, value: string, opts: { nx: true; ex: number }): Promise<unknown>`, un `getRedis()` lazy que crea `Redis.fromEnv()` solo si no existe (idéntico patrón al de clients.ts para no crashear en tests de otros módulos), y `claimDailyReport(dateKey: string, locker: DailyReportLocker = getRedis()): Promise<boolean>` que llama `locker.set(`report:posted:${dateKey}`, '1', { nx: true, ex: LOCK_TTL_SECONDS })` y devuelve `result === 'OK'`. Encabezar con docstring que cite PER-02 y explique que el claim atómico previene posts duplicados ante reintentos/reinvocaciones. Crear `lib/report-lock.test.ts` con un fake locker que registre `[key, value, opts]` y devuelva un valor programado: caso primera-vez (devuelve 'OK') → true y assert de que la clave es `report:posted:2026-06-26` (o el dateKey usado) con nx:true; caso ya-reclamado (devuelve null) → false.</action>
  <verify>
    <automated>npm test -- report-lock && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/report-lock.test.ts` pasa con caso primera-llamada true y segunda-llamada false
    - `grep -c "nx:" lib/report-lock.ts` devuelve >= 1 y aparece `report:posted:`
    - El test afirma que la clave pasada a set tiene el prefijo `report:posted:`
    - `npm run typecheck` verde; el módulo no crashea al importarse sin credenciales Upstash (patrón lazy)
  </acceptance_criteria>
  <done>claimDailyReport reclama el lock diario una sola vez, inyectable y cubierto por tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → endpoint del cron | Cualquiera podría intentar disparar el reporte; isAuthorizedCron es la primera línea |
| Reintentos/reinvocaciones → Redis | Múltiples invocaciones en la misma hora podrían duplicar el post; claimDailyReport lo evita |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Spoofing | isAuthorizedCron | mitigate | Solo Bearer <CRON_SECRET> exacto autoriza; secret vacío nunca autoriza; tests cubren ausente/incorrecto |
| T-04-03 | Denial of service / duplicación | claimDailyReport | mitigate | SET NX EX atómico; segunda llamada del día devuelve false y el handler omite la corrida |
| T-04-SC | Tampering | npm installs | accept | No se agregan dependencias (se reusa @upstash/redis ya presente) |
</threat_model>

<verification>
- `npm test` global pasa sin regresiones
- `npm run typecheck` verde bajo strict + noUncheckedIndexedAccess
</verification>

<success_criteria>
- isReportHour es DST-safe y solo true a la hora local configurada (SCH-01)
- isAuthorizedCron rechaza disparos externos sin CRON_SECRET (SCH-02)
- claimDailyReport garantiza un único post por día (PER-02)
</success_criteria>

<output>
Create `.planning/phases/04-block-kit-report-daily-cron/04-02-SUMMARY.md` when done
</output>
