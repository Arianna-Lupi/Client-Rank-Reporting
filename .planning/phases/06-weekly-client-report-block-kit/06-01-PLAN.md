---
phase: 06-weekly-client-report-block-kit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/weekly-report.ts
  - lib/weekly-report.test.ts
autonomous: true
requirements: [RPT-07, RPT-08, RPT-09]
must_haves:
  truths:
    - "getWeeklyClientReport compone fetchDailyMetrics(21d) -> resolveWeeklyWindow -> aggregateWeek(actual/previa) -> computeWeeklyDeltas y devuelve los deltas semana-a-semana por métrica (RPT-07)"
    - "getWeeklyClientReport consulta fetchPageClicks para la ventana actual y la previa y produce la lista rankeada por delta absoluto de clics vía rankUrlClickDeltas (insumo de RPT-08/RPT-09)"
    - "El resultado WeeklyClientReport es una unión discriminada total (ok / insufficient_data / no_data / error) que nunca lanza"
    - "La variante error es secret-free: nunca interpola el error capturado (mismo patrón que report.ts)"
    - "Todos los fetchers son inyectables vía un objeto deps, de modo que cada rama se prueba offline sin red ni credenciales"
    - "Si la consulta de clics por URL falla, el reporte degrada a lista de URLs vacía pero conserva las métricas (ok), en vez de fallar todo el reporte"
  artifacts:
    - path: "lib/weekly-report.ts"
      provides: "getWeeklyClientReport(siteUrl, deps?) + tipo WeeklyClientReport + tipo WeeklyReportDeps"
      contains: "export async function getWeeklyClientReport"
    - path: "lib/weekly-report.test.ts"
      provides: "Suite vitest offline: ok, insufficient_data, no_data, error, secret-safety y degradación de URLs, con deps mockeados"
      contains: "describe('getWeeklyClientReport"
  key_links:
    - from: "lib/weekly-report.ts"
      to: "lib/weekly.ts (resolveWeeklyWindow, sliceWindow, aggregateWeek, computeWeeklyDeltas, rankUrlClickDeltas)"
      via: "reuso de la capa pura de Phase 5 — sin duplicar cómputo"
      pattern: "resolveWeeklyWindow|aggregateWeek|computeWeeklyDeltas|rankUrlClickDeltas"
    - from: "lib/weekly-report.ts"
      to: "lib/gsc.ts (fetchDailyMetrics, fetchPageClicks)"
      via: "fetchers por defecto inyectables vía deps"
      pattern: "fetchDailyMetrics|fetchPageClicks"
---

<objective>
Construir `lib/weekly-report.ts`: el orquestador de datos del reporte semanal por cliente, análogo semanal de `report.ts`. `getWeeklyClientReport(siteUrl, deps?)` compone la capa pura de Phase 5 (`lib/weekly.ts`) con los fetchers de `lib/gsc.ts` en una unión discriminada, total y never-throwing `WeeklyClientReport` (ok / insufficient_data / no_data / error). Cablea fetchDailyMetrics(ventana de 21 días) -> resolveWeeklyWindow -> aggregateWeek(actual)/aggregateWeek(previa) -> computeWeeklyDeltas para las métricas WoW (RPT-07), y fetchPageClicks para las dos ventanas resueltas -> rankUrlClickDeltas para la lista rankeada de URLs (insumo de RPT-08/RPT-09). Todos los fetchers son inyectables vía un objeto `deps` para que cada rama se pruebe offline.

Purpose: Aislar el cableado semanal detrás de tests offline para que la Phase 6 de render (06-02) sea presentación delgada. Es el análogo semanal de `getClientReport` y reutiliza la capa pura en vez de reescribirla.
Output: `lib/weekly-report.ts`, `lib/weekly-report.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-weekly-client-report-block-kit/06-CONTEXT.md
@.planning/REQUIREMENTS.md
@lib/report.ts
@lib/report.test.ts
@lib/weekly.ts
@lib/gsc.ts

<interfaces>
<!-- Contratos congelados de Phase 5 que este orquestador consume. NO modificar weekly.ts, gsc.ts ni metrics.ts. -->

De lib/weekly.ts (reusar tal cual):
```typescript
export interface WeeklyWindow {
  currentStart: string; currentEnd: string;   // 'YYYY-MM-DD'
  previousStart: string; previousEnd: string;
}
export type WeeklyWindowResult =
  | { status: 'ok'; window: WeeklyWindow; anchor: string }
  | { status: 'insufficient_data'; distinctDays: number };
export interface UrlClickDelta { url: string; current: number; previous: number; delta: number; isNew: boolean; }

export function resolveWeeklyWindow(rows: ReadonlyArray<DailyMetricRow>, windowDays?: number): WeeklyWindowResult;
export function sliceWindow(rows: ReadonlyArray<DailyMetricRow>, start: string, end: string): DailyMetricRow[];
export function aggregateWeek(rows: ReadonlyArray<DailyMetricRow>): WeeklyAggregate;
export function computeWeeklyDeltas(current: WeeklyAggregate, previous: WeeklyAggregate): MetricDeltas;
export function rankUrlClickDeltas(current: ReadonlyMap<string, number>, previous: ReadonlyMap<string, number>): UrlClickDelta[];
```

De lib/gsc.ts (fetchers por defecto):
```typescript
export function fetchDailyMetrics(siteUrl: string, opts?: FetchDailyOptions, query?: GscQueryFn): Promise<DailyMetricRow[]>;
// opts.windowDays default 14 -> aquí pasar { windowDays: 21 } para cubrir 2 semanas + lag
export function fetchPageClicks(siteUrl: string, startDate: string, endDate: string, query?: GscQueryFn): Promise<Map<string, number>>;
```

De lib/report.ts (patrón a espejar):
```typescript
export type ClientReport =
  | { status: 'ok'; date: string; deltas: MetricDeltas }
  | { status: 'insufficient_data'; date?: string }
  | { status: 'no_data' }
  | { status: 'error'; message: string };
// error nunca interpola el error capturado (secret-free).
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getWeeklyClientReport + unión discriminada WeeklyClientReport + tests offline</name>
  <files>lib/weekly-report.ts, lib/weekly-report.test.ts</files>
  <read_first>lib/report.ts (patrón de unión discriminada, try/catch secret-free, fetcher inyectable con default — líneas 15-58), lib/report.test.ts (patrón de fakes inyectados, aserciones exactas por rama, prueba de secret-safety), lib/weekly.ts (resolveWeeklyWindow/sliceWindow/aggregateWeek/computeWeeklyDeltas/rankUrlClickDeltas — líneas 78-202), lib/gsc.ts (firmas de fetchDailyMetrics y fetchPageClicks — líneas 132-198)</read_first>
  <behavior>
    - deps inyectados con rows de dos semanas completas -> status 'ok'; deltas === computeWeeklyDeltas(aggregateWeek(actual), aggregateWeek(previa)); window === el WeeklyWindow resuelto; urls === rankUrlClickDeltas(mapaActual, mapaPrevia).
    - fetchDaily resuelve 0 filas -> status 'no_data' (sin campo window/deltas/urls).
    - fetchDaily resuelve pocas filas (resolveWeeklyWindow devuelve insufficient_data) -> status 'insufficient_data'.
    - fetchDaily lanza -> status 'error' con message no vacío; la llamada RESUELVE (nunca rechaza).
    - Secret-safety: un token plantado en el error de fetchDaily nunca aparece en message (RPT-04/V7 heredado).
    - Un throw no-Error de fetchDaily también se maneja -> variante error genérica.
    - Degradación de URLs: fetchPageClicks lanza (o resuelve mapas vacíos) -> el reporte sigue siendo 'ok' con urls === [] y las métricas intactas (no colapsa todo el reporte por fallo de la dimensión page).
    - fetchPageClicks se invoca con las fechas EXACTAS de la ventana resuelta (currentStart/currentEnd para la actual, previousStart/previousEnd para la previa) — verificado con un spy que captura los argumentos.
    - getWeeklyClientReport nunca lanza para ninguna combinación de entradas (es total).
  </behavior>
  <action>Crear `lib/weekly-report.ts` con un docstring de encabezado que cite RPT-07/RPT-08/RPT-09 y explique que es el análogo semanal de report.ts. Definir e exportar el tipo `WeeklyReportDeps` con dos fetchers opcionales inyectables: `fetchDaily?: (siteUrl: string) => Promise<DailyMetricRow[]>` y `fetchPageClicks?: (siteUrl: string, startDate: string, endDate: string) => Promise<Map<string, number>>`. Definir e exportar la unión discriminada total `WeeklyClientReport`: la variante `ok` lleva `window: WeeklyWindow`, `deltas: MetricDeltas` y `urls: UrlClickDelta[]` (la lista rankeada COMPLETA — la Phase 6 de render corta top 3); las variantes `insufficient_data`, `no_data` y `error` (con `message: string`) espejan report.ts. Implementar `getWeeklyClientReport(siteUrl: string, deps: WeeklyReportDeps = {})`: (1) resolver los defaults — `fetchDaily` por defecto una lambda `(s) => fetchDailyMetrics(s, { windowDays: 21 })` y `fetchPageClicks` por defecto la función homónima de gsc.ts (importarla con alias para no chocar el nombre del campo deps); (2) envolver la llamada a fetchDaily en try/catch — cualquier throw devuelve `{ status: 'error', message: 'No se pudo obtener datos de GSC' }` SIN interpolar el error capturado (secret-leak guard, igual que report.ts); (3) si rows.length === 0 devolver `{ status: 'no_data' }`; (4) llamar resolveWeeklyWindow(rows); si devuelve insufficient_data, devolver `{ status: 'insufficient_data' }`; (5) con la ventana ok, calcular `currentRows = sliceWindow(rows, window.currentStart, window.currentEnd)` y `previousRows = sliceWindow(rows, window.previousStart, window.previousEnd)`, luego `deltas = computeWeeklyDeltas(aggregateWeek(currentRows), aggregateWeek(previousRows))`; (6) obtener los clics por URL de ambas ventanas dentro de su PROPIO try/catch: si cualquiera de las dos llamadas a fetchPageClicks lanza, degradar a `Map` vacíos y continuar (las métricas ya están calculadas y no deben perderse por un fallo de la dimensión page) — usar las fechas exactas de la ventana resuelta; (7) `urls = rankUrlClickDeltas(currentClicks, previousClicks)`; (8) devolver `{ status: 'ok', window, deltas, urls }`. No usar el tipo `any`; importar todos los tipos con `import type`. Crear `lib/weekly-report.test.ts` con vitest siguiendo el estilo de report.test.ts: helpers `row(date, over)` y constructores de deps con fakes que resuelven rows/mapas fijos y otros que rechazan (throwing y no-Error). Cubrir cada rama del bloque behavior, incluir la prueba de secret-safety con token plantado, la prueba de degradación de URLs, y una prueba con spy que afirme que fetchPageClicks recibió las fechas exactas de las dos ventanas. Nunca invocar los defaults reales de gsc.ts — siempre inyectar deps, para que la suite corra 100% offline.</action>
  <verify>
    <automated>npx vitest run lib/weekly-report.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/weekly-report.test.ts` pasa con casos para ok, insufficient_data, no_data, error, secret-safety, throw no-Error y degradación de URLs
    - El test de error afirma que un token plantado NO aparece en `message`
    - El test de ok afirma `deltas` deep-equal a computeWeeklyDeltas de los mismos fixtures y `urls` deep-equal a rankUrlClickDeltas de los mismos mapas
    - Un test con spy afirma que fetchPageClicks fue llamado con (currentStart,currentEnd) y (previousStart,previousEnd) exactos
    - `grep -c "resolveWeeklyWindow\|aggregateWeek\|computeWeeklyDeltas\|rankUrlClickDeltas\|sliceWindow" lib/weekly-report.ts` devuelve >= 5
    - `npm run typecheck` pasa bajo strict + noUncheckedIndexedAccess; el archivo no contiene la palabra `any`
    - La suite no ejerce ninguna red: no aparece `google.searchconsole` ni credenciales en el path de test (todo vía deps inyectados)
  </acceptance_criteria>
  <done>getWeeklyClientReport es total, never-throwing y secret-free; exporta WeeklyClientReport y WeeklyReportDeps; todas las ramas (incluida la degradación de URLs) están cubiertas por tests offline verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| error capturado de GSC -> WeeklyClientReport.message | El texto/stack del error de fetch (puede cargar detalle interno o secretos) no debe propagarse al resultado que la Phase 7 postea |
| fetchPageClicks (dimensión page) -> reporte completo | Un fallo de la consulta secundaria de URLs no debe tumbar las métricas ya calculadas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information disclosure | getWeeklyClientReport (variante error) | mitigate | El catch nunca interpola el error; message es una constante genérica en español; test con token plantado lo verifica |
| T-06-02 | Denial of service | fetchPageClicks (dimensión page) | mitigate | try/catch dedicado que degrada a Map vacío; las métricas sobreviven; test de degradación lo verifica |
| T-06-SC | Tampering | npm installs | accept | Este plan no agrega dependencias (reusa weekly.ts, gsc.ts, metrics.ts existentes) |
</threat_model>

<verification>
- Verificación OFFLINE únicamente (blocker vigente: no hay credenciales vivas de GSC/Slack/Upstash). Toda prueba inyecta deps; el default real de gsc.ts nunca se ejecuta.
- `npm test` global pasa (no regresiona suites de Phase 1-5).
- `npm run typecheck` verde bajo strict + noUncheckedIndexedAccess.
</verification>

<success_criteria>
- getWeeklyClientReport compone la capa semanal en un resultado discriminado total con deltas WoW (RPT-07) y la lista rankeada de URLs (RPT-08/RPT-09), never-throwing y secret-free.
- Todas las ramas cubiertas por tests offline; degradación de URLs preserva las métricas.
</success_criteria>

<output>
Create `.planning/phases/06-weekly-client-report-block-kit/06-01-SUMMARY.md` when done
</output>
