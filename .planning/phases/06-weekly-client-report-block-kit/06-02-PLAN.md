---
phase: 06-weekly-client-report-block-kit
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - lib/slack/blocks.ts
  - lib/slack/blocks.test.ts
autonomous: true
requirements: [RPT-07, RPT-08, RPT-09, RPT-10]
must_haves:
  truths:
    - "buildWeeklyClientReportBlocks(siteUrl, report) renderiza el reporte semanal como Block Kit sin tocar buildClientReportBlocks (el builder diario v1.0 queda intacto y sus tests verdes)"
    - "El header es '📊 {siteUrl} — semana {inicio}…{fin}' usando la ventana actual del reporte"
    - "La sección de métricas muestra Impresiones (tráfico), Clics, CTR y Posición WoW, cada una con indicador de dirección y delta con signo, y enuncia 'vs semana previa' (RPT-07)"
    - "Tras un divider, lista Top 3 URLs que más subieron y luego Top 3 que más bajaron, cortadas de la lista rankeada (RPT-08/RPT-09)"
    - "Formato legible (RPT-10): miles es-ES en clics/impresiones, CTR 2 decimales, posición 1 decimal, delta con signo 1 decimal; URLs como links mrkdwn de solo el path truncado a ~50 chars; delta de clics por URL con signo; 🆕 para URLs nuevas"
    - "Degradación amable: renderiza las URLs que existan; si no hay ninguna, emite una línea de contexto amistosa y omite la sección (nunca una sección vacía)"
    - "Total sobre todas las variantes de WeeklyClientReport, nunca lanza, nunca filtra report.message"
    - "Toda la copy en español que el bot postea está humanizada (sin AI tells, sin em/en dashes, voz natural variada)"
  artifacts:
    - path: "lib/slack/blocks.ts"
      provides: "buildWeeklyClientReportBlocks + helpers de formato semanal (miles es-ES, CTR 2 decimales, link/truncado de URL, delta de clics por URL) junto al builder diario intacto"
      contains: "export function buildWeeklyClientReportBlocks"
    - path: "lib/slack/blocks.test.ts"
      provides: "Cobertura vitest del builder semanal: todas las variantes, formato, truncado, corte top-3, marcador 🆕 y degradación de URLs vacías"
      contains: "describe('buildWeeklyClientReportBlocks"
  key_links:
    - from: "lib/slack/blocks.ts (buildWeeklyClientReportBlocks)"
      to: "lib/weekly-report.ts (WeeklyClientReport, UrlClickDelta, WeeklyWindow)"
      via: "import type del contrato del orquestador 06-01"
      pattern: "WeeklyClientReport|UrlClickDelta"
    - from: "lib/slack/blocks.ts"
      to: "helpers existentes directionLabel/deltaSuffix/contextBlock/SlackBlock"
      via: "reuso de módulo (mismo archivo) sin tocar buildClientReportBlocks"
      pattern: "directionLabel|deltaSuffix|contextBlock"
---

<objective>
Construir la capa de presentación semanal: `buildWeeklyClientReportBlocks(siteUrl, report)` en `lib/slack/blocks.ts`, junto al builder diario v1.0 (`buildClientReportBlocks`) que queda intacto. Renderiza un mensaje Block Kit por cliente (RPT-07) con las 4 métricas WoW e indicador de dirección, un divider, y las Top 3 URLs que más suben y Top 3 que más bajan por delta de clics (RPT-08/RPT-09), con formato legible es-ES (RPT-10), degradación amable cuando faltan URLs, y toda la copy en español humanizada.

Purpose: Convertir el resultado discriminado del orquestador (06-01) en el mensaje semanal listo para que la Phase 7 lo rutee al canal de cada cliente. Función pura y total, probada offline.
Output: `lib/slack/blocks.ts` extendido, `lib/slack/blocks.test.ts` extendido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-weekly-client-report-block-kit/06-CONTEXT.md
@.planning/REQUIREMENTS.md
@lib/slack/blocks.ts
@lib/slack/blocks.test.ts
@lib/weekly.ts

<interfaces>
<!-- Contrato del orquestador 06-01 que este builder consume. NO modificar buildClientReportBlocks ni weekly-report.ts. -->

De lib/weekly-report.ts (06-01):
```typescript
export type WeeklyClientReport =
  | { status: 'ok'; window: WeeklyWindow; deltas: MetricDeltas; urls: UrlClickDelta[] }
  | { status: 'insufficient_data' }
  | { status: 'no_data' }
  | { status: 'error'; message: string };
```

De lib/weekly.ts:
```typescript
export interface WeeklyWindow { currentStart: string; currentEnd: string; previousStart: string; previousEnd: string; }
export interface UrlClickDelta { url: string; current: number; previous: number; delta: number; isNew: boolean; }
```

De lib/metrics.ts:
```typescript
export interface MetricDelta { value: number; previous: number; deltaPct: number | null; improved: boolean; isNew: boolean; }
export interface MetricDeltas { clicks: MetricDelta; impressions: MetricDelta; ctr: MetricDelta; position: MetricDelta; }
// improved ya trae la inversión de posición horneada en Phase 3/5 — NO re-invertir.
// ctr es fracción 0-1; position es promedio.
```

Helpers YA en lib/slack/blocks.ts (privados de módulo, reutilizables por el nuevo builder — NO re-exportar, NO modificar su firma):
`type SlackBlock`, `directionLabel(d)`, `deltaSuffix(d)`, `contextBlock(text)`.
Nota: `formatCtr` existente es de 1 decimal (uso diario) — el builder semanal necesita su PROPIO formateador CTR de 2 decimales (RPT-10, override del usuario). No cambiar el formatCtr diario.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildWeeklyClientReportBlocks + helpers de formato semanal + tests</name>
  <files>lib/slack/blocks.ts, lib/slack/blocks.test.ts</files>
  <read_first>lib/slack/blocks.ts (builder diario completo: SlackBlock, directionLabel líneas 22-27, deltaSuffix 29-36, formatCtr/formatPosition 43-51, contextBlock 68-74, switch total 80-99), lib/weekly.ts (UrlClickDelta y WeeklyWindow, líneas 20-48), lib/slack/blocks.test.ts (patrón de aserciones sobre JSON serializado por rama)</read_first>
  <behavior>
    - status 'ok': devuelve un header cuyo texto es exactamente `📊 {siteUrl} — semana {window.currentStart}…{window.currentEnd}` (usa el carácter elipsis U+2026).
    - Sección de métricas (mrkdwn): cuatro líneas en orden Impresiones, Clics, CTR, Posición; la etiqueta de impresiones aclara "tráfico"; cada línea combina valor formateado + indicador de dirección (vía directionLabel) + delta con signo; el texto enuncia "vs semana previa" una vez.
    - Tras la sección de métricas hay un bloque `divider`.
    - Top 3 subidas: de report.urls (ya rankeada por |delta| desc) filtra delta > 0 y toma 3; cada URL como link mrkdwn `<urlCompleta|/path…>` mostrando solo el path, truncado a ~50 chars con elipsis; el valor por URL es el delta de clics con signo (`+N clics` con U+002B, `−N clics` con el signo menos U+2212); las URLs nuevas (isNew) llevan 🆕.
    - Top 3 bajadas: filtra delta < 0 y toma 3 (ya en orden de magnitud); mismo formato de link y de delta de clics con signo negativo.
    - Formato numérico (RPT-10): clics e impresiones con separador de miles vía Intl.NumberFormat('es-ES'); CTR con 2 decimales (p.ej. 3.24%); posición con 1 decimal; delta % con signo y 1 decimal (reuso de deltaSuffix).
    - Degradación: si report.urls está vacío O no hay ni subidas ni bajadas, se omite la sección de URLs y se emite una línea de contexto amistosa en español (nunca un bloque de sección vacío). Si existen subidas pero no bajadas (o viceversa) se renderiza solo la que exista.
    - status 'insufficient_data' y 'no_data': un único bloque context en español amable con el siteUrl (no error), tipo el patrón diario.
    - status 'error': un único bloque context amable y defensivo; NUNCA incluye report.message.
    - La función es total sobre WeeklyClientReport y nunca lanza.
  </behavior>
  <action>En `lib/slack/blocks.ts` agregar `buildWeeklyClientReportBlocks(siteUrl: string, report: WeeklyClientReport): SlackBlock[]` SIN modificar `buildClientReportBlocks` ni los helpers diarios existentes (sus tests deben seguir verdes). Importar con `import type` los tipos `WeeklyClientReport` desde `../weekly-report.js` y `UrlClickDelta`/`WeeklyWindow` desde `../weekly.js`. Reutilizar los helpers de módulo existentes `directionLabel`, `deltaSuffix` y `contextBlock`. Agregar helpers privados nuevos: (a) un formateador de enteros con miles es-ES usando una instancia de `Intl.NumberFormat('es-ES')` a nivel de módulo (Node full-ICU verificado en CONTEXT) para clics/impresiones; (b) un formateador CTR de 2 decimales (multiplica la fracción por 100, toFixed(2), símbolo %) distinto del formatCtr diario; (c) un formateador de posición de 1 decimal (puede reusar el patrón toFixed(1)); (d) un helper que dado un `UrlClickDelta` construya el link mrkdwn — extrae el path de la URL (usar `new URL(u.url).pathname` con fallback a la cadena cruda si el parseo falla), lo trunca a ~50 chars agregando elipsis U+2026 cuando excede, y emite `<urlCompleta|pathTruncado>`; (e) un helper que formatee el delta de clics por URL como `+N clics` / `−N clics` usando el signo menos U+2212 para negativos y anteponiendo 🆕 cuando `isNew`. Implementar un `switch` total sobre `report.status`: para 'ok' construir [header, section de métricas, divider, (opcional) section de subidas, (opcional) section de bajadas, (opcional) contextBlock de degradación]; el header usa el rango de la ventana actual; la sección de métricas lista las 4 métricas en orden con etiqueta "Impresiones (tráfico)", "Clics", "CTR", "Posición", cada una `valor directionLabel deltaSuffix`, e incluye la aclaración "vs semana previa"; derivar `risers = report.urls.filter(u => u.delta > 0).slice(0,3)` y `droppers = report.urls.filter(u => u.delta < 0).slice(0,3)`; si ambos vacíos, agregar un contextBlock amistoso (sin datos de URLs por ahora) y omitir las secciones de URLs; si hay risers, una section titulada tipo "Top 3 que más subieron"; si hay droppers, una section titulada tipo "Top 3 que más bajaron". Para 'insufficient_data' y 'no_data': un único contextBlock en español amable con el siteUrl. Para 'error': un único contextBlock defensivo que NO incluya report.message. No usar `any`; sin Slack SDK. Encabezar la nueva función con un docstring que cite RPT-07/RPT-08/RPT-09/RPT-10 y aclare que el builder diario queda intacto. Toda la copy nueva se escribe en español natural (será pulida en la Task 2 con el humanizer). Extender `lib/slack/blocks.test.ts` con un `describe('buildWeeklyClientReportBlocks')`: un caso 'ok' con >6 URLs de subida y bajada mezcladas (incluida una isNew y una con path largo >50 chars) que afirme — header con `semana` y el rango, presencia de las 4 etiquetas, "vs semana previa", exactamente 3 risers y 3 droppers en el JSON, separador de miles es-ES (p.ej. un valor con punto de miles), CTR con 2 decimales, 🆕 presente, un link mrkdwn `<http...|/...>` con path truncado y elipsis, y el delta de clics por URL con signo; un caso 'ok' con `urls: []` que afirme ausencia de secciones de URL y presencia del contextBlock de degradación; casos 'insufficient_data', 'no_data' y 'error' que afirmen un solo contextBlock con el siteUrl y (en error) la ausencia de report.message.</action>
  <verify>
    <automated>npx vitest run lib/slack/blocks.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/slack/blocks.test.ts` pasa incluyendo el describe nuevo Y los tests diarios previos (buildClientReportBlocks) siguen verdes
    - El test 'ok' afirma exactamente 3 risers y 3 droppers cuando hay más de 3 de cada uno
    - El test afirma separador de miles es-ES, CTR con 2 decimales, 🆕, link mrkdwn con path truncado a ~50 chars con elipsis, y delta de clics por URL con signo
    - El test 'ok' con urls vacío afirma que no hay secciones de URL y sí un contextBlock amistoso
    - El test 'error' afirma que el JSON NO contiene report.message
    - `grep -c "buildClientReportBlocks" lib/slack/blocks.ts` sigue devolviendo >= 1 y su firma no cambió (builder diario intacto)
    - `grep -c "es-ES" lib/slack/blocks.ts` >= 1 (Intl.NumberFormat es-ES presente)
    - `npm run typecheck` pasa bajo strict + noUncheckedIndexedAccess; sin `any`, sin imports de @slack
  </acceptance_criteria>
  <done>buildWeeklyClientReportBlocks es pura y total, renderiza métricas WoW + top-3 subidas/bajadas con formato es-ES y degradación amable, sin tocar el builder diario; todas las variantes cubiertas por tests verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Humanizar toda la copy en español del reporte semanal (hard rule)</name>
  <files>lib/slack/blocks.ts, lib/slack/blocks.test.ts</files>
  <read_first>~/.claude/skills/humanizer/SKILL.md (aplicar la skill de humanización), lib/slack/blocks.ts (todas las cadenas en español que buildWeeklyClientReportBlocks postea: header, etiquetas de métricas, títulos de subidas/bajadas, sufijos "clics", "vs semana previa", líneas de degradación y de estados sin datos/error)</read_first>
  <action>Invocar la Skill humanizer (`~/.claude/skills/humanizer/SKILL.md`) y aplicarla a TODA cadena en español que el bot postee desde `buildWeeklyClientReportBlocks` y sus helpers en `lib/slack/blocks.ts`: header, etiquetas de métricas, aclaración "vs semana previa", títulos de las secciones Top 3 subidas/bajadas, sufijo "clics", la línea de degradación cuando no hay URLs, y los mensajes de los estados insufficient_data / no_data / error. Requisitos duros: sin AI tells, sin em/en dashes en la prosa (el guion largo — y el guion medio – están prohibidos en el texto; el signo menos U+2212 del delta de clics y la elipsis U+2026 del header/truncado SÍ se conservan porque son notación, no prosa), voz natural y variada, español neutral (sin voseo). NO tocar `buildClientReportBlocks` (builder diario v1.0) ni ningún string fuera del reporte semanal. Si algún ajuste de copy cambia una subcadena que un test afirma literalmente, actualizar la aserción correspondiente en `lib/slack/blocks.test.ts` para que siga verificando el marcador/etiqueta correcto sin re-romper la cobertura. Mantener intactos los emojis funcionales (📊, 🆕, 🟢/🔴 y flechas) y el marcado mrkdwn.</action>
  <verify>
    <automated>npx vitest run lib/slack/blocks.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - La skill humanizer fue aplicada a toda la copy semanal; ninguna cadena posteada contiene em dash (—) ni en dash (–) en la prosa
    - `grep -c "—\|–" lib/slack/blocks.ts` devuelve 0 (sin guiones largos/medios; el header usa … y los deltas usan −, que no matchean estos caracteres)
    - Todos los tests de `lib/slack/blocks.test.ts` (semanal + diario) pasan tras el ajuste de copy
    - `npm run typecheck` verde
    - buildClientReportBlocks (diario) permanece sin cambios
  </acceptance_criteria>
  <done>Toda la copy en español del reporte semanal está humanizada (sin AI tells, sin em/en dashes en prosa, voz natural neutral); tests verdes y builder diario intacto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report.message -> mensaje de Slack | El texto de error del orquestador (posible detalle interno) no debe propagarse al canal en la variante error |
| URL cruda de GSC -> link mrkdwn | La URL se envuelve como link; el path se trunca — no se ejecuta ni interpola contenido no controlado más allá de mostrarse como texto/link |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Information disclosure | buildWeeklyClientReportBlocks (variante error) | mitigate | El bloque de contexto nunca incluye report.message; test lo verifica |
| T-06-04 | Tampering | Render de URL como link mrkdwn | accept | La URL proviene de GSC (fuente confiable de la propiedad del cliente); se muestra como link con path truncado, sin ejecución |
| T-06-SC | Tampering | npm installs | accept | Este plan no agrega dependencias (Intl es nativo; sin instalaciones nuevas) |
</threat_model>

<verification>
- Verificación OFFLINE únicamente (blocker vigente: sin credenciales vivas de GSC/Slack/Upstash). El builder es puro; los tests afirman sobre el JSON serializado, sin red.
- `npm test` global pasa (no regresiona buildClientReportBlocks ni las suites de Phase 1-5, incluida 06-01).
- `npm run typecheck` verde bajo strict + noUncheckedIndexedAccess.
</verification>

<success_criteria>
- buildWeeklyClientReportBlocks renderiza métricas WoW con dirección (RPT-07), Top 3 subidas (RPT-08) y Top 3 bajadas (RPT-09) con formato legible es-ES, truncado y separadores (RPT-10), sin tocar el builder diario.
- Degradación amable cuando faltan URLs; total y never-throwing; nunca filtra report.message.
- Toda la copy en español posteada está humanizada (hard rule).
</success_criteria>

<output>
Create `.planning/phases/06-weekly-client-report-block-kit/06-02-SUMMARY.md` when done
</output>
