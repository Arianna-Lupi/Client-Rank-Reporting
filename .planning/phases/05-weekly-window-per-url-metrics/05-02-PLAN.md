---
phase: 05-weekly-window-per-url-metrics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/gsc.ts
  - lib/gsc.test.ts
autonomous: true
requirements: [GSC-06]
must_haves:
  truths:
    - "fetchPageClicks consulta Search Analytics con dimensions:['page'], dataState 'final' y rowLimit 250 para una ventana de fechas dada"
    - "fetchPageClicks devuelve un mapa URL → clics agregados, con la query inyectable para tests offline"
    - "Filas sin key de URL o con clicks nulo se manejan sin romper (skip / coalesce a 0)"
    - "fetchDailyMetrics y sus tests siguen intactos (misma GscQueryFn extendida de forma no-breaking)"
  artifacts:
    - path: "lib/gsc.ts"
      provides: "fetchPageClicks(siteUrl, startDate, endDate, query?) con el mismo patrón DI + null-coalescing de fetchDailyMetrics"
      contains: "export async function fetchPageClicks"
    - path: "lib/gsc.test.ts"
      provides: "Tests de mapeo de filas page → mapa de clics vía mock GscQueryFn (sin red)"
      contains: "describe('fetchPageClicks"
  key_links:
    - from: "lib/gsc.ts fetchPageClicks"
      to: "GscQueryFn inyectable (defaultQuery)"
      via: "misma DI que fetchDailyMetrics para correr offline"
      pattern: "query: GscQueryFn = defaultQuery"
    - from: "lib/gsc.ts requestBody"
      to: "dimensions:['page'] + rowLimit 250"
      via: "consulta per-URL de una ventana"
      pattern: "dimensions: \\['page'\\]"
---

<objective>
Extender `lib/gsc.ts` con `fetchPageClicks(siteUrl, startDate, endDate, query?)` (GSC-06): una consulta a Search Analytics con dimensión `page` que devuelve los clics por URL de una propiedad para una ventana de fechas explícita. Sigue el mismo patrón de inyección de dependencias (`GscQueryFn` por defecto `defaultQuery`) y null-coalescing que `fetchDailyMetrics`, de modo que la suite corre offline con un mock y sin credenciales vivas.

Purpose: Aporta el insumo per-URL que la Phase 5 (join/ranking en weekly.ts) y la Phase 6 (top 3 subidas/bajadas) consumen. La resolución de las fechas de ventana la hace `resolveWeeklyWindow` (05-01); esta función recibe start/end ya resueltos.
Output: `fetchPageClicks` en `lib/gsc.ts` y sus tests en `lib/gsc.test.ts`. No modifica el comportamiento de `fetchDailyMetrics`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-weekly-window-per-url-metrics/05-CONTEXT.md
@.planning/REQUIREMENTS.md
@lib/gsc.ts
@lib/gsc.test.ts

<interfaces>
<!-- Contratos existentes en lib/gsc.ts a reutilizar/extender. -->

From lib/gsc.ts:
```typescript
export interface RawAnalyticsRow {
  keys?: string[] | null;   // con dimensions:['page'], keys[0] es la URL
  clicks?: number | null;
  impressions?: number | null;
  ctr?: number | null;
  position?: number | null;
}

// requestBody actual: { startDate, endDate, dimensions, dataState }.
// Se le AGREGA rowLimit?: number (opcional → cambio no-breaking; fetchDailyMetrics no lo setea).
export type GscQueryFn = (params: {
  siteUrl: string;
  requestBody: { startDate: string; endDate: string; dimensions: string[]; dataState: string; rowLimit?: number };
}) => Promise<{ data: { rows?: RawAnalyticsRow[] | null } }>;

const defaultQuery: GscQueryFn = (params) => getSearchConsole().searchanalytics.query(params);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gsc.ts — fetchPageClicks(siteUrl, startDate, endDate, query?) (GSC-06)</name>
  <files>lib/gsc.ts, lib/gsc.test.ts</files>
  <read_first>lib/gsc.ts (líneas 85-162: FetchDailyOptions, RawAnalyticsRow, GscQueryFn, defaultQuery, fetchDailyMetrics — replicar exactamente el patrón DI + null-coalescing + skip de rows sin key), lib/gsc.test.ts (líneas 1-20 el helper mockQuery + estructura describe)</read_first>
  <behavior>
    - fetchPageClicks('sc-domain:x.com', '2026-06-14', '2026-06-20', mock) llama la query una vez con siteUrl exacto y requestBody = { startDate:'2026-06-14', endDate:'2026-06-20', dimensions:['page'], dataState:'final', rowLimit:250 }.
    - Mapea cada fila a (keys[0] → clicks): devuelve un Map<string,number> URL→clics.
    - Filas sin keys / keys[0] vacío se saltan; clicks nulo coalesce a 0.
    - Si una misma URL aparece en más de una fila, se suman los clics (defensivo).
    - rows null/undefined → Map vacío, sin throw.
  </behavior>
  <action>Extender `lib/gsc.ts` (sin tocar `fetchDailyMetrics` ni `filterReadableSites`). Primero ampliar el tipo `GscQueryFn` agregando `rowLimit?: number` al `requestBody` (opcional, no-breaking). Luego agregar `export async function fetchPageClicks(siteUrl: string, startDate: string, endDate: string, query: GscQueryFn = defaultQuery): Promise<Map<string, number>>`. En el cuerpo: llamar `query({ siteUrl, requestBody: { startDate, endDate, dimensions: ['page'], dataState: 'final', rowLimit: 250 } })`; tomar `res.data.rows ?? []`; construir un `Map<string, number>` iterando: `const url = r.keys?.[0]; if (url === undefined || url === '') continue; map.set(url, (map.get(url) ?? 0) + (r.clicks ?? 0));`. Encabezar con docstring que cite GSC-06, explique que usa la MISMA `getSearchConsole()`/auth `GSC_SA_KEY_B64` que la fetch diaria, que las fechas de ventana vienen resueltas de fuera (resolveWeeklyWindow, 05-01), y que la query está aislada tras el param inyectable para correr sin API viva. Ampliar `lib/gsc.test.ts` con un `describe('fetchPageClicks (GSC-06)')` reusando el helper `mockQuery`: caso default (assert dimensions:['page'], dataState:'final', rowLimit:250, siteUrl y las fechas exactas pasadas), mapeo de filas a Map con clics correctos, filas sin key saltadas, clicks nulo → 0, URL repetida sumada, rows null → Map vacío.</action>
  <verify>
    <automated>npm test -- gsc && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/gsc.test.ts` pasa, incluyendo el nuevo describe fetchPageClicks y los describe existentes (filterReadableSites, fetchDailyMetrics) sin regresión
    - `grep -c "dimensions: \['page'\]" lib/gsc.ts` devuelve >= 1 y aparece `rowLimit: 250`
    - `grep -c "export async function fetchPageClicks" lib/gsc.ts` devuelve 1
    - `npm run typecheck` verde bajo strict + noUncheckedIndexedAccess
  </acceptance_criteria>
  <done>fetchPageClicks consulta la dimensión page con rowLimit 250 y dataState final para una ventana dada, devuelve un mapa URL→clics inyectable y probado offline, sin alterar fetchDailyMetrics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GSC API → función | Respuesta externa (filas page) entra al mapeo; filas malformadas deben saltarse sin romper |
| Credenciales SA → runtime | La auth vive en GSC_SA_KEY_B64 vía getSearchConsole(); no se loggea ni se interpola en resultados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering / datos corruptos | fetchPageClicks mapeo | mitigate | Skip de filas sin key, coalesce de clicks nulo a 0, rows null → Map vacío; tests cubren cada caso |
| T-05-04 | Information disclosure | auth SA / errores | accept | Reusa getSearchConsole() existente; sin logging del key ni interpolación de errores en salidas |
| T-05-SC | Tampering | npm/pip installs | accept | No se agregan dependencias; reusa googleapis ya presente |
</threat_model>

<verification>
- `npm test` global pasa sin regresiones (fetchDailyMetrics y filterReadableSites intactos)
- `npm run typecheck` verde bajo strict + noUncheckedIndexedAccess
- Sin credenciales vivas: la query se prueba con mock GscQueryFn (blocker de creds GSC/Slack/Upstash vigente; no hay e2e en esta fase)
</verification>

<success_criteria>
- fetchPageClicks obtiene clics por URL de una propiedad para una ventana de fechas vía dimensión page (GSC-06)
- La función es inyectable y totalmente unit-testeada offline, con el mismo patrón que fetchDailyMetrics
- fetchDailyMetrics y su suite quedan intactos (extensión no-breaking de GscQueryFn)
</success_criteria>

<output>
Create `.planning/phases/05-weekly-window-per-url-metrics/05-02-SUMMARY.md` when done
</output>
