---
phase: 07-per-client-channel-routing-roster
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - lib/commands/setchannel.ts
  - lib/commands/setchannel.test.ts
  - lib/commands/getdata.ts
  - lib/commands/getdata.test.ts
  - lib/commands/router.ts
  - lib/commands/router.test.ts
autonomous: true
requirements: [CH-02, CMD-09]
must_haves:
  truths:
    - "/setchannel <cliente> <#canal> persists the resolved channel id for an active client and confirms in humanized Spanish"
    - "/setchannel rejects an unknown/inactive client and a malformed channel reference with a clear humanized Spanish error"
    - "/getdata <cliente> posts that client's WEEKLY report to its MAPPED channel and replies ephemerally naming the destination"
    - "/getdata errors clearly when the client is unknown/inactive or has no mapped channel, and never fans out or posts an ephemeral preview"
    - "dispatch routes /setchannel and /getdata; unknown commands still return the fixed unsupported string"
  artifacts:
    - path: "lib/commands/setchannel.ts"
      provides: "/setchannel handler: parse client+channel, validate active + channel ref, persist channel id"
      exports: ["handleSetChannel", "SetChannelDeps"]
      min_lines: 40
    - path: "lib/commands/getdata.ts"
      provides: "/getdata handler: resolve active client, look up mapped channel, post weekly report, ephemeral confirm"
      exports: ["handleGetData", "GetDataDeps"]
      min_lines: 40
    - path: "lib/commands/router.ts"
      provides: "dispatch extended with /setchannel and /getdata; CommandDeps widened"
      contains: "/setchannel"
  key_links:
    - from: "lib/commands/setchannel.ts"
      to: "lib/channels.ts setClientChannel"
      via: "persist resolved channel id after active-set validation"
      pattern: "setClientChannel"
    - from: "lib/commands/getdata.ts"
      to: "lib/slack/post.ts postMessage"
      via: "post weekly blocks to the client's mapped channel"
      pattern: "postMessage"
    - from: "lib/commands/router.ts"
      to: "handleSetChannel / handleGetData"
      via: "dispatch switch cases"
      pattern: "case '/setchannel'|case '/getdata'"
---

<objective>
Add the two v1.1 commands to the existing dispatcher: `/setchannel <cliente> <#canal>` (CH-02) sets a client's destination channel, and `/getdata <cliente>` (CMD-09) posts that client's weekly report to its mapped channel on demand. Both mirror the established add/list handler + CommandDeps + dispatch pattern and are fully offline-tested with injected fakes.

Purpose: This is the operator-facing surface of the channel map. /setchannel is how channels get assigned before the weekly cron can route; /getdata is Juan's explicit on-demand trigger that sends a client's data to that client's channel (not an ephemeral preview, not a fan-out).
Output: lib/commands/setchannel.ts, lib/commands/getdata.ts, extended router.ts, all with offline vitest coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-per-client-channel-routing-roster/07-CONTEXT.md
@.planning/phases/07-per-client-channel-routing-roster/07-01-SUMMARY.md

<interfaces>
<!-- Contracts to build against. From the codebase + Plan 07-01 output. -->

From lib/commands/router.ts (the dispatch + CommandDeps pattern to extend):
```typescript
export type CommandDeps = AddDeps & RemoveDeps & ListDeps;
export async function dispatch(command: string | null, arg: string, deps: CommandDeps = {}): Promise<string>;
// switch on command: '/list' | '/add' | '/remove' | default -> 'Comando no soportado.'
```

From lib/commands/add.ts (handler shape: trim arg, resolve, DI deps, Spanish reply):
```typescript
export interface AddDeps { listReadableSites?: () => Promise<GscSite[]>; writer?: ActiveClientWriter; }
export async function handleAdd(arg: string, deps: AddDeps = {}): Promise<string>;
```

From lib/clients.ts:
```typescript
export async function getActiveClients(reader?: ActiveClientReader): Promise<Set<string>>;
```

From lib/site-match.ts (resolve free-form ref against canonical candidates):
```typescript
export type ResolveResult =
  | { kind: 'match'; siteUrl: string } | { kind: 'multiple'; candidates: string[] } | { kind: 'none' };
export function resolveSiteRef(input: string, candidates: ReadonlyArray<string>): ResolveResult;
```

From lib/channels.ts (Plan 07-01):
```typescript
export async function getClientChannel(siteUrl: string, reader?: ChannelMapReader): Promise<string | null>;
export async function setClientChannel(siteUrl: string, channelId: string, writer?: ChannelMapWriter): Promise<void>;
```

From lib/weekly-report.ts (Phase 6 — reuse, do not reimplement):
```typescript
export async function getWeeklyClientReport(siteUrl: string, deps?: WeeklyReportDeps): Promise<WeeklyClientReport>;
// status: 'ok' | 'insufficient_data' | 'no_data' | 'error'
```

From lib/slack/blocks.ts + lib/slack/post.ts (Phase 6 — reuse):
```typescript
export function buildWeeklyClientReportBlocks(siteUrl: string, report: WeeklyClientReport): SlackBlock[];
export async function postMessage(channel: string, blocks: SlackBlock[], deps?: PostDeps): Promise<void>;
```

Slack channel-mention format in the command `text`: when a channel is picked, Slack sends
the escaped mention `<#C0123ABCD|canal-name>`. Persist the `C…` id, never the `#name`.
</interfaces>

<standing_blocker>
No live Slack/GSC/Upstash creds. ALL verification is offline unit-test level with injected
fakes (no real postMessage, no real Redis). E2e is deferred to the credentials unblock.
</standing_blocker>

<humanization_rule>
HARD RULE: every Spanish string these handlers reply or post MUST be humanized — no AI
tells, no em/en dashes in prose, neutral Spanish, no voseo. This is an explicit acceptance
item on every copy string below.
</humanization_rule>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: /setchannel handler (CH-02)</name>
  <files>lib/commands/setchannel.ts, lib/commands/setchannel.test.ts</files>
  <behavior>
    - Empty/one-token arg returns a humanized usage hint naming both operands
    - A malformed channel reference (bare #name with no id, or garbage) returns a clear humanized error telling the user to pick the channel from Slack's autocomplete
    - A client not in the active set returns a humanized "no está en el reporte" error (validated against getActiveClients)
    - A valid active client + well-formed <#C…|name> persists the C-id via setClientChannel and returns a humanized success naming the client and channel
    - All deps (getActiveClients reader, channel writer, channel-ref parse) injected; no live services
  </behavior>
  <action>Create lib/commands/setchannel.ts exporting `SetChannelDeps` (optional `reader?: ActiveClientReader`, `writer?: ChannelMapWriter`) and `handleSetChannel(arg, deps = {})` returning the ephemeral Spanish reply, following the add.ts shape (per CH-02 + 07-CONTEXT). Parse `arg` into exactly two tokens: the client ref and the channel ref (split on whitespace, first token = client, remainder trimmed = channel). Add a small pure `parseChannelId(ref): string | null` that extracts the id from Slack's escaped mention `<#(C[A-Z0-9]+)(\|[^>]*)?>` and returns null for anything else (including a bare `#name`, which carries no id). Validation order: (1) if fewer than two tokens -> usage hint; (2) parse the channel ref, null -> humanized "no reconozco ese canal, elígelo desde el autocompletado de Slack"; (3) resolve the client ref against the ACTIVE set members (`resolveSiteRef(ref, [...await getActiveClients(deps.reader)])`) — `none` -> "«ref» no está en el reporte, agrégalo con /add primero", `multiple` -> list the candidates; (4) on match, `setClientChannel(match.siteUrl, channelId, deps.writer)` and reply success naming the canonical siteUrl and the channel. v1.1 only ASSIGNS — no clear/off path. Every reply string humanized (neutral Spanish, no voseo, no em/en dashes, no secret interpolation). Write setchannel.test.ts with injected fake active reader and a recording channel writer covering: usage hint, malformed channel, inactive client, and the happy path (assert the exact C-id and canonical siteUrl reach the writer).</action>
  <verify>
    <automated>npx vitest run lib/commands/setchannel.test.ts</automated>
  </verify>
  <done>handleSetChannel validates active client + channel ref, persists the parsed C-id via setClientChannel, all copy is humanized Spanish, and every test case passes offline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /getdata handler (CMD-09)</name>
  <files>lib/commands/getdata.ts, lib/commands/getdata.test.ts</files>
  <behavior>
    - Empty arg returns a humanized usage hint
    - Unknown/inactive client returns a humanized error and posts nothing
    - Active client with NO mapped channel returns a humanized "no tiene canal asignado, usa /setchannel" error and posts nothing
    - Active client WITH a mapped channel: getWeeklyClientReport -> buildWeeklyClientReportBlocks -> postMessage(mappedChannel, blocks); ephemeral reply confirms the destination channel
    - Never fans out to all clients and never posts an ephemeral preview of the report
    - post/report/channel-lookup all injected; no real Slack/GSC
  </behavior>
  <action>Create lib/commands/getdata.ts exporting `GetDataDeps` (optional `reader?: ActiveClientReader`, `channelReader?: ChannelMapReader`, `getReport?: typeof getWeeklyClientReport`, `post?: typeof postMessage`) and `handleGetData(arg, deps = {})` (per CMD-09 + 07-CONTEXT). Steps: trim arg, empty -> humanized usage hint; resolve the ref against the ACTIVE set (`resolveSiteRef` over `getActiveClients(deps.reader)`) — `none`/`multiple` handled with humanized errors like /setchannel; on match, `getClientChannel(siteUrl, deps.channelReader)` — null -> humanized "«siteUrl» todavía no tiene canal asignado, asígnalo con /setchannel"; otherwise call `getWeeklyClientReport(siteUrl, ...)` (default the real Phase 6 orchestrator), build blocks with `buildWeeklyClientReportBlocks(siteUrl, report)`, and `postMessage(channelId, blocks)` (default the real post). Reply ephemerally confirming the destination, e.g. "Listo, publiqué el reporte semanal de *siteUrl* en su canal." Do NOT post an ephemeral preview of the blocks and do NOT iterate other clients — this is single-client, real-post-to-mapped-channel only. Keep it secret-free (never interpolate report.message or the token). Every reply humanized. Write getdata.test.ts with injected fakes: fake active reader, fake channel reader (mapped and unmapped cases), a fake getReport returning an 'ok' report, and a recording fake post; assert the post targets the mapped channel id and that the unmapped/unknown paths post nothing.</action>
  <verify>
    <automated>npx vitest run lib/commands/getdata.test.ts</automated>
  </verify>
  <done>handleGetData posts the weekly report to the mapped channel and confirms the destination ephemerally, errors humanely on unknown/inactive/unmapped clients, never fans out or previews, and passes offline with fakes.</done>
</task>

<task type="auto">
  <name>Task 3: wire /setchannel and /getdata into the router (CH-02, CMD-09)</name>
  <files>lib/commands/router.ts, lib/commands/router.test.ts</files>
  <action>Extend lib/commands/router.ts: import handleSetChannel/SetChannelDeps and handleGetData/GetDataDeps, widen `CommandDeps` to `AddDeps & RemoveDeps & ListDeps & SetChannelDeps & GetDataDeps`, and add `case '/setchannel': return handleSetChannel(arg, deps);` and `case '/getdata': return handleGetData(arg, deps);` to the dispatch switch. Leave the default 'Comando no soportado.' branch intact. Update lib/commands/router.test.ts adding cases: /setchannel routes to handleSetChannel (assert a channel-set reply given fake active reader + writer + a valid <#C…|name> arg) and /getdata routes to handleGetData (assert the mapped-channel confirmation given fakes), plus confirm the unsupported branch still returns the fixed string. Note: api/slack/command.ts already forwards `command` + `text` to dispatch with no deps, so production wiring needs no shell change — the handlers supply real defaults; do not modify the api shell.</action>
  <verify>
    <automated>npx vitest run lib/commands/router.test.ts</automated>
  </verify>
  <done>dispatch routes both new commands to their handlers, CommandDeps composes their deps, the unsupported branch is unchanged, and router.test.ts passes offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slack user -> command handler | Untrusted `text` (client ref + channel ref) crosses here; parsed and validated before any persist/post |
| handler -> Slack chat.postMessage | Proactive post uses the bot token; only the mapped channel id and rendered blocks are sent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | setchannel channel arg | mitigate | parseChannelId only accepts the escaped `<#C…|name>` mention and extracts the C-id; bare/garbage refs rejected, raw text never persisted |
| T-07-05 | Spoofing/Tampering | client ref in both commands | mitigate | resolveSiteRef against the ACTIVE set only; unknown/inactive refs error out, never resolve to an off-roster property |
| T-07-06 | Information disclosure | getdata error/reply copy | mitigate | Never interpolate report.message, the bot token, or Slack error internals into replies (reuse Phase 6 secret-free guard) |
| T-07-07 | Elevation/abuse | /getdata fan-out or preview | mitigate | Single-client only, posts solely to the mapped channel; no iteration over all clients, no ephemeral block preview |
| T-07-SC | Tampering | npm installs | accept | No new packages introduced in this plan |
</threat_model>

<verification>
- `npm run typecheck` passes (CommandDeps composes cleanly, no `any`).
- `npx vitest run lib/commands/` all green.
- Grep confirms routing: `grep -nE "case '/(setchannel|getdata)'" lib/commands/router.ts` returns both.
- Humanization check: read each Spanish string in setchannel.ts/getdata.ts — no em/en dashes in prose, neutral Spanish, no voseo, no secret interpolation.
- No live Slack/GSC/Upstash touched by any test (all deps injected).
</verification>

<success_criteria>
- CH-02: /setchannel validates active client + channel ref and persists the resolved channel id, with humanized confirms/errors.
- CMD-09: /getdata posts the reused Phase 6 weekly report to the client's mapped channel and confirms the destination ephemerally, erroring clearly on unknown/inactive/unmapped clients; no fan-out, no preview.
- Both commands are routed via dispatch and covered by offline unit tests; live e2e deferred to the creds unblock.
</success_criteria>

<output>
Create `.planning/phases/07-per-client-channel-routing-roster/07-02-SUMMARY.md` when done.
</output>
