---
phase: 07-per-client-channel-routing-roster
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/channels.ts
  - lib/channels.test.ts
  - lib/config.ts
  - lib/config.test.ts
  - lib/schedule.ts
  - lib/schedule.test.ts
  - scripts/seed-roster.ts
  - scripts/seed-roster.test.ts
autonomous: true
requirements: [CH-01, SCH-04, CFG-01]
must_haves:
  truths:
    - "A client's Slack channel id can be written to and read back from the Redis HASH clients:channels"
    - "The whole client->channel map can be read at once via hgetall"
    - "The scheduler reports true only when the local weekday in REPORT_TZ equals REPORT_DOW (default Monday)"
    - "getConfig() exposes reportDow, defaulting to Monday when REPORT_DOW is missing or invalid"
    - "The initial roster list (deltacloudz.com, felipevergara.co, childrenchic.com, fhcaorlando.com) is defined and nicmafia is absent"
  artifacts:
    - path: "lib/channels.ts"
      provides: "Injectable client->channel map (get/set/getAll) over Redis HASH clients:channels"
      contains: "clients:channels"
      exports: ["getClientChannel", "setClientChannel", "getAllChannels", "ChannelMapReader", "ChannelMapWriter"]
      min_lines: 40
    - path: "lib/schedule.ts"
      provides: "isReportDow day-of-week gate alongside isReportHour/reportDateKey"
      exports: ["isReportHour", "reportDateKey", "isReportDow"]
    - path: "lib/config.ts"
      provides: "reportDow config field with fail-safe default"
      contains: "reportDow"
    - path: "scripts/seed-roster.ts"
      provides: "Idempotent active-set roster seed (SADD) with an exported ROSTER list for offline verification"
      exports: ["ROSTER", "seedRoster"]
  key_links:
    - from: "lib/channels.ts"
      to: "clients:channels"
      via: "hget / hgetall / hset on the CHANNELS_KEY"
      pattern: "clients:channels"
    - from: "scripts/seed-roster.ts"
      to: "lib/clients.ts addClient"
      via: "SADD of each canonical roster siteUrl"
      pattern: "addClient|sadd"
---

<objective>
Build the Wave-1 standalone primitives that Waves 2 wire together: the Redis client->channel map (CH-01), the weekly day-of-week scheduling gate plus its config (SCH-04 primitives), and the idempotent roster seed script (CFG-01). Every artifact is pure or injectable and fully offline-testable — no live Slack, GSC or Upstash.

Purpose: These are the foundation modules with no upstream consumers inside this phase. Isolating them in Wave 1 lets the command plan (07-02) and the cron rewire (07-03) run in parallel afterward against stable contracts.
Output: lib/channels.ts, isReportDow + reportDow config, scripts/seed-roster.ts, each with offline vitest coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-per-client-channel-routing-roster/07-CONTEXT.md

<interfaces>
<!-- Mirror these exactly. Extracted from the codebase; no exploration needed. -->

From lib/clients.ts (the pattern lib/channels.ts must mirror — ACTIVE_KEY const,
lazy getRedis(), injectable Reader/Writer, canonical-siteUrl contract):
```typescript
const ACTIVE_KEY = 'clients:active';
export interface ActiveClientReader { smembers(key: string): Promise<string[]>; }
export interface ActiveClientWriter {
  sadd(key: string, ...members: string[]): Promise<number>;
  srem(key: string, ...members: string[]): Promise<number>;
}
let redis: Redis | undefined;
function getRedis(): Redis { if (redis === undefined) redis = Redis.fromEnv(); return redis; }
export async function getActiveClients(reader: ActiveClientReader = getRedis()): Promise<Set<string>>;
export async function addClient(siteUrl: string, writer: ActiveClientWriter = getRedis()): Promise<boolean>;
```

From lib/schedule.ts (existing pure gates; isReportDow joins these):
```typescript
export function isReportHour(now: Date, tz: string, hour: number): boolean;
export function reportDateKey(now: Date, tz: string): string;
```

From lib/config.ts (fail-fast loader; reportHour is the exact precedent for reportDow):
```typescript
export interface AppConfig { /* … */ readonly reportHour: number; }
function resolveReportHour(): number; // parses REPORT_HOUR, [0,23], default 9
export function getConfig(): AppConfig;
export function resetConfigCache(): void; // tests reset between env mutations
```

`@upstash/redis` HASH surface used here: `hset(key, { [field]: value }): Promise<number>`,
`hget(key, field): Promise<string | null>`, `hgetall(key): Promise<Record<string,string> | null>`.
</interfaces>

<standing_blocker>
No live GSC/Slack/Upstash credentials exist yet (Phase 1 Task 2 is still credential-gated).
ALL verification in this phase is offline unit-test level. Live seed execution and any
end-to-end run are deferred to the credentials unblock. Do NOT require live execution to
mark a task done.
</standing_blocker>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/channels.ts — Redis HASH client->channel map (CH-01)</name>
  <files>lib/channels.ts, lib/channels.test.ts</files>
  <behavior>
    - setClientChannel writes the channel id under field=siteUrl in HASH clients:channels (hset called with { [siteUrl]: channelId })
    - getClientChannel returns the stored channel id for a mapped siteUrl, and null for an unmapped one
    - getAllChannels returns a Map of every field->value from hgetall, and an EMPTY Map when hgetall returns null (no key yet)
    - reader/writer are injectable fakes in tests; no live Redis, no env read
  </behavior>
  <action>Create lib/channels.ts mirroring lib/clients.ts verbatim in structure (per CH-01 and 07-CONTEXT decisions): a `const CHANNELS_KEY = 'clients:channels'`; a lazy `getRedis()` using `Redis.fromEnv()` so importing never crashes without creds; a `ChannelMapReader` interface exposing `hget(key, field)` and `hgetall(key)`; a `ChannelMapWriter` interface exposing `hset(key, obj)`. Export `getClientChannel(siteUrl, reader = getRedis())` returning `Promise<string | null>`, `setClientChannel(siteUrl, channelId, writer = getRedis())` returning `Promise<void>` (or the hset count), and `getAllChannels(reader = getRedis())` returning `Promise<Map<string,string>>` built from `hgetall` (empty Map when it returns null). Store the resolved channel ID as the value and the canonical siteUrl as the field — never store the `#name`. Do not log any secret. Write lib/channels.test.ts with injected fake reader/writer (recording calls), covering: set persists under the right key/field, get hit and miss, getAll populated and getAll-on-null-returns-empty-Map.</action>
  <verify>
    <automated>npx vitest run lib/channels.test.ts</automated>
  </verify>
  <done>channels.ts exports the three functions plus the two interfaces, uses HASH key clients:channels, and all channels.test.ts cases pass offline with fakes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: isReportDow gate + reportDow config (SCH-04 primitives)</name>
  <files>lib/schedule.ts, lib/schedule.test.ts, lib/config.ts, lib/config.test.ts</files>
  <behavior>
    - isReportDow(now, tz, dow) is true only when the LOCAL weekday in tz equals dow (0=Sunday … 6=Saturday), derived via Intl (DST-safe, deterministic from the passed `now`)
    - A fixed UTC instant that is Monday-local returns true for dow=1 and false for dow=0
    - resolveReportDow parses REPORT_DOW: integer in [0,6] wins; missing/blank/out-of-range/non-integer falls back to 1 (Monday)
    - getConfig().reportDow reflects REPORT_DOW; resetConfigCache() lets tests re-read a mutated env
  </behavior>
  <action>Extend lib/schedule.ts with `export function isReportDow(now: Date, tz: string, dow: number): boolean`, following the exact Intl pattern of isReportHour: use `Intl.DateTimeFormat('en-US', { timeZone: tz, weekday: 'short' })` (or `formatToParts`) to get the local weekday, map short names {Sun:0,Mon:1,Tue:2,Wed:3,Thu:4,Fri:5,Sat:6} to an index, and compare to `dow`. Keep it deterministic (reads only `now`). Extend lib/config.ts: add `readonly reportDow: number` to AppConfig, add a `DEFAULT_REPORT_DOW = 1` const and a `resolveReportDow()` helper modeled on resolveReportHour() (accept integer [0,6], else default 1), and set `reportDow: resolveReportDow()` inside getConfig(). Add lib/config.test.ts (or extend if present) covering reportDow default and a valid override, using resetConfigCache() between env mutations. Add schedule.test.ts cases for isReportDow across a Monday-local instant and a non-matching day.</action>
  <verify>
    <automated>npx vitest run lib/schedule.test.ts lib/config.test.ts</automated>
  </verify>
  <done>isReportDow gates on local weekday correctly, getConfig().reportDow defaults to Monday (1) and honors REPORT_DOW, all new/updated tests pass offline.</done>
</task>

<task type="auto">
  <name>Task 3: scripts/seed-roster.ts — idempotent roster seed (CFG-01)</name>
  <files>scripts/seed-roster.ts, scripts/seed-roster.test.ts</files>
  <action>Create the new `scripts/` dir and scripts/seed-roster.ts. Export a `ROSTER` array of the canonical GSC siteUrls to seed and a `seedRoster(writer?)` function that SADDs each into the active set (reuse lib/clients.ts `addClient`, which already writes to clients:active; pass the injectable writer through so the seed is unit-testable offline). Roster per CFG-01 and 07-CONTEXT: deltacloudz.com, felipevergara.co, childrenchic.com, fhcaorlando.com — nicmafia is NOT included. Resolve/document the canonical siteUrl form for each: seed as `sc-domain:<host>` (domain-property form) UNLESS a URL-prefix form is required, and add a top-of-file comment stating that the exact canonical strings must be confirmed against `sites.list` before the live run (the standing blocker defers live execution). Add an inline comment on the childrenchic.com entry noting it is seeded as-is pending Arianna's domain rename. The seed loads ONLY the active set — it does NOT preload channel mappings (those come later via /setchannel). Make the module runnable (an `if` main-guard that calls seedRoster() with real deps when executed directly) but keep verification offline: scripts/seed-roster.test.ts asserts (a) ROSTER contains exactly the four hosts and excludes nicmafia, (b) seedRoster with a fake writer issues one SADD per roster entry to clients:active, (c) re-running with a writer that returns 0 (already present) does not throw — idempotent. Do NOT require live Upstash execution.</action>
  <verify>
    <automated>npx vitest run scripts/seed-roster.test.ts</automated>
  </verify>
  <done>ROSTER lists the four canonical hosts (no nicmafia) with the childrenchic rename note; seedRoster is idempotent and offline-testable; live execution is explicitly deferred to the creds unblock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module -> Redis | Channel ids and roster siteUrls written to Upstash; only canonical/validated values persisted |
| env -> config | REPORT_DOW is untrusted external input parsed by resolveReportDow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | REPORT_DOW parsing | mitigate | resolveReportDow accepts only integers [0,6]; anything else falls back to Monday (1), never crashes the run |
| T-07-02 | Information disclosure | channels.ts / seed logging | mitigate | Never log the Redis token or values; no secret interpolation, mirroring lib/clients.ts |
| T-07-03 | Denial of service | seed re-run | accept | SADD is idempotent; re-running only re-adds existing members (no duplicate state) |
| T-07-SC | Tampering | npm installs | accept | No new packages — all deps (@upstash/redis, vitest) already installed and version-pinned |
</threat_model>

<verification>
- `npm run typecheck` passes (no `any`, strict types on the Redis HASH surfaces).
- `npx vitest run lib/channels.test.ts lib/schedule.test.ts lib/config.test.ts scripts/seed-roster.test.ts` all green.
- Grep confirms the HASH key: `grep -n "clients:channels" lib/channels.ts` returns a match.
- No live network in any test (all deps injected as fakes).
</verification>

<success_criteria>
- CH-01: lib/channels.ts persists client->channel under HASH clients:channels with injectable get/set/getAll, offline-tested.
- SCH-04 (primitives): isReportDow gates on local weekday; getConfig().reportDow defaults to Monday and honors REPORT_DOW.
- CFG-01: scripts/seed-roster.ts seeds the four-client active roster idempotently (nicmafia excluded, childrenchic rename-noted), verified offline; live run deferred.
- All verification is offline unit-test level per the standing blocker.
</success_criteria>

<output>
Create `.planning/phases/07-per-client-channel-routing-roster/07-01-SUMMARY.md` when done.
</output>
