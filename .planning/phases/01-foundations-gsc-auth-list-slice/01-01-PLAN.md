---
phase: 01-foundations-gsc-auth-list-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vercel.json
  - .gitignore
  - .env.example
  - vitest.config.ts
  - lib/config.ts
autonomous: false
requirements: [SCH-03]
user_setup:
  - service: slack
    why: "Slash command /list + verificación de firma HMAC (CMD-03, CMD-05)"
    env_vars:
      - name: SLACK_SIGNING_SECRET
        source: "Slack App → Basic Information → App Credentials → Signing Secret"
      - name: SLACK_CHANNEL_ID
        source: "Slack → canal destino → Copy link (ID que empieza con C...)"
    dashboard_config:
      - task: "Crear Slack App; agregar slash command /list apuntando a la Request URL del preview de Vercel (se setea en el Plan 03)"
        location: "api.slack.com/apps → Create New App → Slash Commands"
  - service: google-cloud
    why: "Service Account para auth a GSC sin intervención manual (GSC-01)"
    env_vars:
      - name: GSC_SA_KEY_B64
        source: "GCP → IAM → Service Accounts → Keys → crear JSON key → base64 del archivo completo"
    dashboard_config:
      - task: "Crear proyecto GCP, habilitar Search Console API, crear Service Account + JSON key"
        location: "console.cloud.google.com → APIs & Services / IAM"
      - task: "Agregar el email de la SA (<sa>@<project>.iam.gserviceaccount.com) como usuario en CADA propiedad GSC a reportar (GSC es per-property, no domain-wide)"
        location: "search.google.com/search-console → Settings → Users and permissions"
  - service: upstash
    why: "Redis para persistir la lista de clientes activos entre invocaciones (PER-01)"
    env_vars:
      - name: UPSTASH_REDIS_REST_URL
        source: "Upstash Console → Redis database → REST API → UPSTASH_REDIS_REST_URL"
      - name: UPSTASH_REDIS_REST_TOKEN
        source: "Upstash Console → Redis database → REST API → UPSTASH_REDIS_REST_TOKEN"
    dashboard_config:
      - task: "Crear una instancia de Upstash Redis (free tier)"
        location: "console.upstash.com → Create Database"

must_haves:
  truths:
    - "Las tres dependencias externas (Slack App, GCP Service Account con su email concedido en las propiedades GSC, Upstash Redis) están provisionadas y sus credenciales viven en .env.local"
    - "El proyecto compila con TypeScript 5.x y las dependencias oficiales instaladas (googleapis, @upstash/redis, @vercel/node, vitest)"
    - "Toda la config sensible se lee desde env vars con fail-fast; nada hardcodeado"
  artifacts:
    - path: "package.json"
      provides: "Dependencias pineadas + scripts (test, typecheck)"
      contains: "googleapis"
    - path: "tsconfig.json"
      provides: "Config TS estricta, target Node"
    - path: ".env.example"
      provides: "Inventario documentado de todas las env vars (sin valores reales)"
      contains: "GSC_SA_KEY_B64"
    - path: ".gitignore"
      provides: "Excluye .env.local y .vercel"
      contains: ".env.local"
    - path: "lib/config.ts"
      provides: "Lectura/validación fail-fast de env vars"
      min_lines: 20
  key_links:
    - from: "lib/config.ts"
      to: "process.env"
      via: "lectura validada de SLACK_SIGNING_SECRET, GSC_SA_KEY_B64, SLACK_CHANNEL_ID, UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN, REPORT_TZ"
      pattern: "process\\.env\\."
---

<objective>
Provisionar las tres dependencias externas del proyecto (Slack App, Google Cloud Service Account, Upstash Redis), montar el scaffold del proyecto TypeScript/Vercel, y centralizar la lectura de configuración sensible en `lib/config.ts` con fail-fast. Es la base del walking skeleton: sin estos servicios reales y este andamiaje, el slice `/list` no puede verificarse end-to-end.

Purpose: Establecer el suelo compartido que reusan los Planes 02-03 (y las Fases 2-4). Cubre SCH-03 (config por env) y deja listos los secretos para GSC-01/CMD-05/PER-01.
Output: `package.json`, `tsconfig.json`, `vercel.json`, `.gitignore`, `.env.example`, `lib/config.ts`, dependencias instaladas, `.env.local` poblado con credenciales reales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-gsc-auth-list-slice/01-CONTEXT.md
@.planning/phases/01-foundations-gsc-auth-list-slice/01-RESEARCH.md
@.planning/phases/01-foundations-gsc-auth-list-slice/SKELETON.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Provisionar Slack App, GCP Service Account y Upstash Redis</name>
  <read_first>
    - .planning/phases/01-foundations-gsc-auth-list-slice/01-RESEARCH.md (secciones "Environment Availability" y "Pitfall 4: SA no agregada como usuario")
    - .planning/phases/01-foundations-gsc-auth-list-slice/SKELETON.md (Architectural Decisions)
  </read_first>
  <what-built>
    Esta es una tarea de provisión humana — Claude no puede crear cuentas externas. El ejecutor presenta los pasos; el humano los realiza y pega los valores en `.env.local`.
  </what-built>
  <how-to-verify>
    1. Slack: crear App en api.slack.com/apps. Copiar el **Signing Secret** (Basic Information → App Credentials) → `SLACK_SIGNING_SECRET`. Copiar el ID del canal destino → `SLACK_CHANNEL_ID`. (La Request URL del slash command `/list` se configura en el Plan 03, tras el primer deploy.)
    2. Google Cloud: crear proyecto, habilitar "Search Console API", crear una Service Account, generar una **JSON key**. Convertir el archivo JSON completo a base64 (`base64 -i key.json` en macOS) → `GSC_SA_KEY_B64`. NO extraer la private key por separado ni usar `\n` escapados.
    3. Google Search Console: para cada propiedad a reportar, agregar el email de la SA (`<sa>@<project>.iam.gserviceaccount.com`) como usuario en Settings → Users and permissions. Sin este paso `sites.list` devuelve la propiedad como `siteUnverifiedUser` o no la devuelve.
    4. Upstash: crear una base Redis (free tier). Copiar `UPSTASH_REDIS_REST_URL` y `UPSTASH_REDIS_REST_TOKEN` desde la sección REST API.
    5. Pegar los 6 valores en `.env.local` (se crea en este plan; NUNCA se commitea).
  </how-to-verify>
  <acceptance_criteria>
    - `.env.local` existe en la raíz y contiene valores reales (no placeholders) para: `SLACK_SIGNING_SECRET`, `SLACK_CHANNEL_ID`, `GSC_SA_KEY_B64`, `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`.
    - El email de la SA aparece como usuario en al menos una propiedad GSC real (confirmado por el humano).
    - `GSC_SA_KEY_B64` decodifica a un JSON válido con campos `client_email` y `private_key` (el humano confirma; verificación completa de auth ocurre en el Plan 03 en preview).
  </acceptance_criteria>
  <resume-signal>Escribe "listo" cuando los 6 valores estén en .env.local y la SA esté concedida en las propiedades, o describe qué falta</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold del proyecto + lib/config.ts</name>
  <read_first>
    - .planning/phases/01-foundations-gsc-auth-list-slice/01-RESEARCH.md (secciones "Standard Stack", "Recommended Project Structure", "Nota TypeScript")
    - package.json (si existe, para no sobrescribir)
  </read_first>
  <files>package.json, tsconfig.json, vercel.json, .gitignore, .env.example, vitest.config.ts, lib/config.ts</files>
  <action>
    Crear `package.json` declarando dependencias pineadas (NO instalar todavía — eso es Task 3): `googleapis@173`, `@upstash/redis@1` como dependencies; `typescript@5` (pin a la última 5.x estable, NO 6.x por A1/RESEARCH), `@types/node`, `@vercel/node@5`, `vitest` como devDependencies. Scripts: `"test": "vitest run"`, `"typecheck": "tsc --noEmit"`. `"type": "module"`.
    Crear `tsconfig.json` con `strict: true`, `module`/`moduleResolution` adecuados para ESM + `@vercel/node`, `target` ES2022, `types: ["node"]`, `noEmit` implícito (Vercel compila).
    Crear `vercel.json` mínimo (puede declarar `functions` para la ruta `api/slack/command.ts`, runtime Node — NO Edge).
    Crear `vitest.config.ts` mínimo que habilite resolución de tests TypeScript/ESM (incluir `lib/**/*.test.ts` en `test.include`, environment `node`). Debe permitir que `npx vitest run` descubra y ejecute los `.test.ts` sin config extra de Babel.
    Crear `.gitignore` incluyendo al menos `node_modules`, `.env.local`, `.env*.local`, `.vercel`, `dist`.
    Crear `.env.example` documentando TODAS las env vars con comentario de origen y SIN valores reales: `SLACK_SIGNING_SECRET`, `SLACK_CHANNEL_ID`, `GSC_SA_KEY_B64`, `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`, `REPORT_TZ` (default documentado p.ej. `America/Mexico_City`; lo consume Fase 4 pero se documenta ahora por SCH-03).
    Crear `lib/config.ts`: módulo que lee y valida las env vars requeridas en cold start con fail-fast (lanza un error claro nombrando la var faltante si alguna no está definida). Exponer un objeto/función tipada (p.ej. `getConfig()` o un export `config`) con campos para cada var. `SLACK_SIGNING_SECRET`, `GSC_SA_KEY_B64`, `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN` son requeridas; `REPORT_TZ` opcional con default. NUNCA loguear los valores (V7).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tee /dev/stderr; test -f vitest.config.ts && grep -q "GSC_SA_KEY_B64" .env.example && grep -q "REPORT_TZ" .env.example && grep -qE "^\.env\.local" .gitignore && grep -q "process.env" lib/config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lista `googleapis`, `@upstash/redis`, `typescript` (rango `5.x`, no `6`), `@vercel/node`, `vitest`; scripts `test` y `typecheck` presentes.
    - `.gitignore` contiene una línea que excluye `.env.local` y otra `.vercel`.
    - `.env.example` documenta las 6 vars (incluida `REPORT_TZ`) sin valores reales.
    - `lib/config.ts` lee cada var requerida vía `process.env` y lanza error nombrando la var si falta; no contiene `console.log` de secretos.
    - `vitest.config.ts` existe y `npx vitest run` resuelve/ejecuta los `.test.ts` en TypeScript/ESM (verificado por el Plan 01-02 al correr los tests).
    - `tsc --noEmit` no reporta errores de tipo en `lib/config.ts`.
  </acceptance_criteria>
  <done>Scaffold creado, config fail-fast tipado, typecheck limpio</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Gate de legitimidad de paquetes + npm install</name>
  <read_first>
    - .planning/phases/01-foundations-gsc-auth-list-slice/01-RESEARCH.md (sección "Package Legitimacy Audit")
    - package.json (creado en Task 2)
  </read_first>
  <what-built>
    slopcheck NO corrió en el entorno de research, así que per protocolo el primer `npm install` se gatea con verificación humana de los nombres de paquete antes de instalar. Los cuatro paquetes son SDKs oficiales (Google, Upstash, Vercel) con millones de descargas, pero el gate es barato y obligatorio. La versión de `vitest` está marcada `[ASSUMED]` y debe confirmarse aquí. Este gate NUNCA es auto-aprobable.
  </what-built>
  <how-to-verify>
    1. Verificar en el registro oficial que cada nombre de paquete es correcto y existe:
       - npmjs.com/package/googleapis (repo googleapis/google-api-nodejs-client)
       - npmjs.com/package/@upstash/redis (repo upstash/redis-js)
       - npmjs.com/package/@vercel/node (repo vercel/vercel)
       - npmjs.com/package/vitest (repo vitest-dev/vitest) — confirmar la versión estable actual y fijarla en package.json
    2. Confirmar que ningún nombre tiene typosquatting (guiones, scopes o sufijos extraños).
    3. Tras aprobación, ejecutar `npm install`.
  </how-to-verify>
  <acceptance_criteria>
    - El humano confirmó explícitamente los 4 nombres de paquete contra sus páginas oficiales de npm (T-01-SC mitigado).
    - `vitest` quedó fijado a una versión estable confirmada en `package.json`.
    - `npm install` completó sin errores; existe `node_modules/` y `package-lock.json`.
    - `node -e "require('googleapis'); require('@upstash/redis')"` no lanza (o equivalente con import ESM).
  </acceptance_criteria>
  <resume-signal>Escribe "aprobado" tras verificar los nombres de paquete; el ejecutor corre npm install. Describe cualquier discrepancia.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → registro npm | Instalación de dependencias de terceros (riesgo supply-chain) |
| repo ↔ secretos | Credenciales sensibles deben vivir solo en env/`.env.local`, nunca commiteadas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `npm install` (googleapis, @upstash/redis, @vercel/node, vitest) | mitigate | Task 3 — checkpoint humano blocking-human verifica nombres contra npmjs.com antes de instalar; todos SDKs oficiales |
| T-01-01 | Information Disclosure | `.env.local` / `GSC_SA_KEY_B64` | mitigate | `.gitignore` excluye `.env.local` y `.vercel`; `.env.example` sin valores reales; `lib/config.ts` nunca loguea secretos (V7/V14) |
| T-01-02 | Spoofing | Service Account sin grant per-property | accept | Documentado en README/CONTEXT; sin el grant, `sites.list` excluye la propiedad — falla visible, no silenciosa |
</threat_model>

<verification>
- `tsc --noEmit` limpio sobre el scaffold.
- `.env.local` poblado con 6 valores reales; `.env.local` y `.vercel` en `.gitignore`.
- `npm install` exitoso tras el gate humano de paquetes.
- `.env.example` documenta las 6 env vars (SCH-03).
</verification>

<success_criteria>
Las tres dependencias externas están provisionadas con credenciales en `.env.local`, el proyecto compila con TS 5.x y deps oficiales instaladas, y toda la config sensible se lee con fail-fast desde env (SCH-03), sin nada hardcodeado ni secretos en el repo.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-gsc-auth-list-slice/01-01-SUMMARY.md` when done
</output>
